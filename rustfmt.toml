edition = "2021"
max_width = 100
tab_spaces = 4
use_small_heuristics = "Max"
newline_style = "Auto"
reorder_imports = true

# Ostale napredne nastavitve so na voljo samo v nightly kanalu
# Če želite uporabiti te funkcije, zaženite:
# cargo +nightly fmt
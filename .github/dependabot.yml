# 🔄 TallyIO Automated Dependency Updates
# Optimized for minimal workflow runs and batch updates

version: 2

updates:
  # 🦀 Rust dependencies (Primary ecosystem)
  - package-ecosystem: "cargo"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      time: "09:00"
      timezone: "UTC"

    # 🎯 Batch update strategy
    open-pull-requests-limit: 3

    # 🏷️ PR configuration
    commit-message:
      prefix: "deps"
      prefix-development: "deps-dev"
      include: "scope"

    # 📋 Labels
    labels:
      - "dependencies"
      - "rust"
      - "automated"

    # 🔍 Allow specific dependency types
    allow:
      - dependency-type: "direct"
      - dependency-type: "indirect"

    # 🚨 Ignore major version updates for critical dependencies
    ignore:
      # Core async runtime - major updates need careful review
      - dependency-name: "tokio"
        update-types: ["version-update:semver-major"]

      # Database driver - major updates need migration planning
      - dependency-name: "sqlx"
        update-types: ["version-update:semver-major"]

      # Blockchain libraries - major updates need thorough testing
      - dependency-name: "ethers"
        update-types: ["version-update:semver-major"]
      - dependency-name: "web3"
        update-types: ["version-update:semver-major"]

    # 📦 Batch all updates together (reduces workflow runs)
    groups:
      # Batch all dependencies together for fewer PRs
      all-dependencies:
        patterns:
          - "*"
        update-types:
          - "minor"
          - "patch"

      # Separate group for major updates (need manual review)
      major-updates:
        patterns:
          - "*"
        update-types:
          - "major"

  # 🔧 GitHub Actions (Monthly updates only)
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
      day: 15
      time: "09:00"
      timezone: "UTC"

    open-pull-requests-limit: 3

    commit-message:
      prefix: "ci"
      include: "scope"

    labels:
      - "dependencies"
      - "github-actions"
      - "automated"
